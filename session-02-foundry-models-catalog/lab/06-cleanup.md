# Cleanup

Nothing to clean up. This session only browses the model catalog and builds a comparison table — no resources are provisioned and nothing is billable.

Keep your comparison table; you'll use your primary model choice as the starting point for Session 3 (Foundry Models II — Deployment & Model Router).
