# 09 — Troubleshooting

Authenticate:

```bash
az login
az account show
```

Upgrade SDK if needed:

```bash
pip install --upgrade "azure-ai-projects>=2.3.0" azure-identity
```

If the agent invents real order facts, remember this lab has no order tool. Strengthen instructions or continue to the next OpenAPI-tool session.
