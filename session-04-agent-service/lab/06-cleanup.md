# 06 — Cleanup

## Steps

1. Delete the test agent (`<firstagent>`) created in `03-implementation-steps.md`, from the New Foundry Agents list.
2. If a tool connection (e.g. Azure AI Search) was created solely for this test, consider whether to delete that connection/resource separately — connections are tied to a backing Azure resource, which may carry its own cost independent of the agent.

